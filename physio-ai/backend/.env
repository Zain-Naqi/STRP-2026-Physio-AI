# Server Config
PORT=5000
NODE_ENV=development

# JWT Secrets
JWT_SECRET=physioai-dev-secret-change-in-production
JWT_EXPIRES_IN=7d

# Database (MySQL — not yet active)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=physioai
